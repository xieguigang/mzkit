Imports System.Runtime.CompilerServices
Imports SubGroup = BioNovoGene.BioDeep.Chemoinformatics.Formula.Formula

Namespace Embedding

    Public Module GroupQuery

        ''' <summary>
        ''' Check of the given sub-group of the atom elements is existsed inside the specific molecule?
        ''' </summary>
        ''' <param name="mol"></param>
        ''' <param name="subgroup"></param>
        ''' <returns></returns>
        <Extension>
        Public Function CheckSubGroup(mol As ChemicalFormula, subgroup As SubGroup) As Boolean

        End Function
    End Module
End Namespace