#Region "Microsoft.ROpen::7085176c814cc787e4113887f5a43cdc, Composition.R"

    # Summaries:

    # element.numbers <- function(composition) {}element.ratio.H_C <- function(composition) {} element.ratio.heteroatom <- function(composition) {}element.probability <- function(composition) {...

#End Region

# Hello, world!
#
# This is an example function named 'hello' 
# which prints 'Hello, world!'.
#
# You can learn more about package authoring with RStudio at:
#
#   http://r-pkgs.had.co.nz/
#
# Some useful keyboard shortcuts for package authoring:
#
#   Build and Reload Package:  'Ctrl + Shift + B'
#   Check Package:             'Ctrl + Shift + E'
#   Test Package:              'Ctrl + Shift + T'

# 对所生成的分子式的原子构成利用黄金七法则进行判定打分该结果是否有效
# 在这个模块之中所有的composition变量对象都是一个list()，名字为原子的符号，对应的值为原子的数量

# 进行规则一打分
element.numbers <- function(composition) {

}

element.ratio.H_C <- function(composition) {

} 

element.ratio.heteroatom <- function(composition) {

}

element.probability <- function(composition) {
    
}
